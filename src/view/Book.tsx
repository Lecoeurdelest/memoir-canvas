/**
 * TASK-015 — R3F scene, book mesh, lighting, camera. Runs on fixture data first, waiting for nothing below it.
 *
 * R5: read from src/store/, never mutate. To write, call src/domain/commands.ts.
 */

export function Book(): JSX.Element | null {
  // TODO(TASK-015)
  return null;
}
