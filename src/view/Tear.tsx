/**
 * TASK-018 — The tear appears while a claim is conflicting; the book cannot close there. It heals on resolution (TASK-019).
 *
 * R5: read from src/store/, never mutate. To write, call src/domain/commands.ts.
 */

export function Tear(): JSX.Element | null {
  // TODO(TASK-018)
  return null;
}
