/**
 * TASK-020 — The relationship constellation. First task to cut if the schedule slips.
 *
 * R5: read from src/store/, never mutate. To write, call src/domain/commands.ts.
 */

export function Constellation(): JSX.Element | null {
  // TODO(TASK-020)
  return null;
}
