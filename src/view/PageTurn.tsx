/**
 * TASK-017 — A rotateY hinge with easing. No physics simulation — that is a time sink.
 *
 * R5: read from src/store/, never mutate. To write, call src/domain/commands.ts.
 */

export function PageTurn(): JSX.Element | null {
  // TODO(TASK-017)
  return null;
}
