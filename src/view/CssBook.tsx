/**
 * TASK-026 — Fallback: the CSS 3D book. Only build it if R3F does not survive the webview. Decide by end of day 3.
 *
 * R5: read from src/store/, never mutate. To write, call src/domain/commands.ts.
 */

export function CssBook(): JSX.Element | null {
  // TODO(TASK-026)
  return null;
}
