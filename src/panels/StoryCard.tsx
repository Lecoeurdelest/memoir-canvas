/**
 * TASK-023 — The bilingual story card. floor_certainty comes from the data, never from the agent.
 *
 * R5: read from src/store/, never mutate. To write, call src/domain/commands.ts.
 */

export function StoryCard(): JSX.Element | null {
  // TODO(TASK-023)
  return null;
}
