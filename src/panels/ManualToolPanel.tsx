/**
 * TASK-006 — Invoke tools by hand when modelContext is absent. Insurance for the demo video.
 *
 * R5: read from src/store/, never mutate. To write, call src/domain/commands.ts.
 */

export function ManualToolPanel(): JSX.Element | null {
  // TODO(TASK-006)
  return null;
}
