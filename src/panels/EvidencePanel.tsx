/**
 * TASK-022 — Sources, verbatim excerpts, supports/contradicts stance.
 *
 * R5: read from src/store/, never mutate. To write, call src/domain/commands.ts.
 */

export function EvidencePanel(): JSX.Element | null {
  // TODO(TASK-022)
  return null;
}
