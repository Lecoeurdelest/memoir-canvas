/**
 * TASK-024 — A log readable by a non-technical person. A judge will open this panel.
 *
 * R5: read from src/store/, never mutate. To write, call src/domain/commands.ts.
 */

export function AuditTrail(): JSX.Element | null {
  // TODO(TASK-024)
  return null;
}
