/**
 * TASK-021 — Five certainty labels, bilingual, never relying on colour alone.
 *
 * R5: read from src/store/, never mutate. To write, call src/domain/commands.ts.
 */

export function CertaintyBadge(): JSX.Element | null {
  // TODO(TASK-021)
  return null;
}
